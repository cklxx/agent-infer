//! OPD-only train command namespace.
//!
//! The legacy non-OPD training, eval, and dataset command modules were deleted
//! in the OPD-only pivot. The `arle train opd` entry is
//! currently a CLI stub in `crates/cli/src/train_cli.rs`; the in-crate OPD
//! command module lands with the next substrate tranche.
