# P0.3 framing correction post 7-layer c20b1ce audit closure

> Per `3fea979` Layer-7 closure:c20b1ce was NO-OP,`12300c5` was actual
> fix(cap=4→8 admission flip)。**Pickup queue P0.3 dispatch directive
> still cites stale framing**。This brief proposes surgical text fixes
> for tomorrow's pickup pass。

## Stale claims in current P0.3(lines 217-258)

### Stale claim #1(line 220-221)
> "Cold-start prefill warmup gap。Single-tick fix unblocks ALL cap=8
> benefit。"

**Audit verdict**:overclaim。"ALL cap=8 benefit"(76→100% turn success)
already came from `12300c5`,not from P0.3。P0.3 only addresses **residual
bimodal variance** if prefill kernel cold-state contributes to it
(unverified per EOD+82 / EOD+90)。

**Corrected framing**:
> "Residual bimodal variance(67% normal / 33% degraded 56% byte-identical
> per `db20d34`)may persist post-12300c5 if first-burst prefill GEMM
> hits cold cublasLt heuristic state。P0.3 prefill warmup pass could
> close this residual,but **gated on Phase 0.5 evidence** that prefill
> is the actual bottleneck(per `3456f8f` recipe)。"

### Stale claim #2(line 246)
> "Root cause: c20b1ce warmup covers DECODE only, not PREFILL cold-start."

**Audit verdict**:wrong。c20b1ce was NO-OP per `8d91d20` /  `3fea979`。Decode
coverage comes from `num_slots` loop in `warmup.rs:75-82`,not from c20b1ce's
max_bs change。In production-default num_slots=8=prefill_cap,decode batches
1..8 are warmed REGARDLESS of c20b1ce。

**Corrected framing**:
> "Root cause hypothesis: warmup covers DECODE batch sizes 1..num_slots
> via the existing slot loop(production-default num_slots=8 already covers
> 1..8 batch sizes regardless of c20b1ce no-op)。Prefill GEMM at varying
> PROMPT lengths(M dimension)is NOT warmed — first burst's prefill
> shape may hit cold cublasLt heuristic state。**Hypothesis**,not yet
> evidenced — Phase 0.5 cheap experiment required first。"

### Stale claim #3(line 255-256)
> "c20b1ce already fixed max_bs to read model.max_concurrent_prefill_requests
> (line 42-43), so DECODE paths are now warmed for batch sizes up to 8"

**Audit verdict**:wrong attribution。c20b1ce did NOT fix anything for
production-default。Decode 1..8 was always warmed via num_slots=8。

**Corrected framing**:
> "Decode paths warmed for batch sizes 1..num_slots via existing slot loop
> in warmup_cuda_graphs。In production-default(num_slots=8),that's batch
> 1..8 — no c20b1ce dependency。"

## Recommended edit(surgical pickup queue patch)

In `docs/plans/codex-pickup-queue-2026-05-09.md`:

**Replace line 220-221**:
```
-Cold-start prefill warmup gap。Single-tick fix unblocks ALL cap=8 benefit。
+Residual bimodal variance(67% normal / 33% degraded per `db20d34`)
+POST-12300c5 may persist if first-burst prefill GEMM cold-state contributes。
+P0.3 prefill warmup pass closes RESIDUAL,gated on Phase 0.5 evidence。
```

**Replace line 246**:
```
-Root cause: c20b1ce warmup covers DECODE only, not PREFILL cold-start.
+Root cause hypothesis: warmup covers DECODE batch sizes 1..num_slots via
+slot loop. Prefill GEMM at varying PROMPT lengths (M dim) NOT warmed.
+Hypothesis, not yet evidenced — Phase 0.5 required first.
```

**Replace line 255-256**:
```
-c20b1ce already fixed max_bs to read model.max_concurrent_prefill_requests
-(line 42-43), so DECODE paths are now warmed for batch sizes up to 8
+Decode paths warmed for 1..num_slots via existing slot loop. In production-
+default num_slots=8, that's 1..8 batch sizes (no c20b1ce dependency,
+per EOD+90 7-layer audit closure 3fea979).
```

## Why surgical not rewrite

P0.3 SOLID gates(Phase 0.5 cheap-experiment + Phase 0.5b grep)were
correctly added in `8b1a913` and remain valid。Effort estimate
80-100 LOC remains valid。Gate progression is sound。Only the **causal
framing of WHY P0.3 is needed** is stale。

Surgical edits preserve 80% of current dispatch directive while fixing
the 20% stale framing。Less churn for tomorrow's pickup。

## Methodology insight

This is anti-pattern #22 in REVERSE form:**after a fix's attribution
is corrected,downstream documents that cited the old attribution
need surgical revision**。

Attribution chain pollution propagates:if doc A says "X was the fix"
and doc B cites doc A,reverting attribution in A doesn't auto-fix
B。Audit must include downstream-citing-document scan,not just the
original claim's commit。

For skill v1.8.0:**anti-pattern #22 expanded sub-rule**:after correcting
a fix's attribution,grep all docs citing the wrong attribution and
update them surgically。

## Cross-references

- `3fea979` Layer-7 closure(12300c5 = real fix)
- `8d91d20` Layer-6 NO-OP finding
- `919c0fb` Layer-2-5 silent-fail logic
- `c20b1ce` no-op cosmetic
- `12300c5` actual fix(cap=4→8)
- `8b1a913` Phase 0.5/0.5b SOLID gates(still valid)
- `3456f8f` cheap-experiment recipe(still valid)
- Pickup queue P0.3 section(lines 217-300)
- §0 first principle:CLAUDE.md "求真务实,追求极致"

## Status

Surgical 3-line edits proposed for tomorrow's pickup pass。If
applied,P0.3 dispatch directive becomes consistent with 7-layer
audit conclusions。

P0.3 itself remains valid pickup item(80-100 LOC,prefill warmup
pass at varying prompt lengths)— just the **motivation framing**
shifts from "wrong attribution" to "hypothesis-grounded uncertainty
gate-resolved by Phase 0.5"。

§0 in action at the META layer:audit conclusions must propagate to
downstream documents,not just be filed in research entries。
Otherwise inconsistency persists between research(corrected)and
plans(stale)。
