# P1.0 self-refinement — batch threshold tightening per R4 #6 lesson

> Codex self-correction during pre-commit review: tightened MarlinW4Hybrid
> dispatch threshold from `batch > 1` to `batch > 8`,explicitly preserving
> W4A16 Marlin path for decode-batched(batch 2-8)scenarios。This is
> empirical application of R4 #6 KILL evidence + anti-pattern #25 lesson。

## Codex's refinement(uncommitted as of EOD+119)

Pre-commit review pivot:
> "我把 threshold 改成 batch > 8,和现有 decode-batched 分界保持一致;
> 默认 OFF 也只拦真正 prefill batch。测试改为 seq_len=16 触发 prefill,
> 同时断言 env-on 下 seq_len=2 仍走 W4A16。"

## Why this refinement matters(empirical chain)

R4 #6 KILL evidence(`fe9ea8a` + `3b9cc06`):**Marlin tensor-core wins
even at batch=4**(decode-batched scenario)。W4A8 launch-overhead savings
do not compensate for losing Marlin's tensor-core utilization at small batches。

→ **Decode-batched(batch 2-8)is NOT a "real prefill" context**,even
though it falls in `batch > 1`。If hybrid prefill override fired at
batch=2-8,it would lose to Marlin per R4 #6 evidence(repeating the
+37% ITL regression class)。

Original P1.0 design `batch > 1`(per my directive at `/tmp/codex-brief-p1.0-hybrid-phase2.txt`)would have been **vulnerable to R4 #6 class regression** at decode-batched scenarios。

## Refined dispatch matrix(post-codex-refinement)

| context | batch range | hybrid path | reason |
|---|---|---|---|
| Decode | `batch == 1` | W4A16 Marlin(MarlinW4Gemm)| production-stable,no W4A8 dependency |
| **Decode-batched** | `batch ∈ 2..=8` | **W4A16 Marlin**(NEW protection)| Marlin tensor-core wins per R4 #6 evidence |
| Real prefill(env-OFF)| `batch > 8` | fail-fast(Phase 1b)| safety preserved |
| Real prefill(env-ON + aligned)| `batch > 8` | MarlinW4Hybrid → W4A8 | hypothesis target,R4 #6 leak prevented |

## Anti-pattern #25 strict-subset check(post-refinement)

- Hypothesis target context:**real prefill batch > 8**(target where W4A8
  launch-overhead savings compensate for tensor-core loss)
- Implementation firing context:**`batch > 8` AND env-on AND aligned**
- Implementation = **strict subset** of hypothesis target ✓
- Decode + decode-batched explicitly excluded(R4 #6 leak class prevented)

Compare R4 #6 implementation:`batch > 1` unconditional → fired at
batch 2-8(decode-batched)+ batch=4096(prefill)→ the decode-batched
firing was the empirical KILL trigger。

P1.0 refined implementation: `batch > 8` AND env-on AND aligned →
fires only at batch=16+(real prefill)→ R4 #6 leak class prevented
by design。

## Bidirectional pattern compounding

Stage progression on P1.0 axis:
1. `aaf0b55` nsys decomp evidence(prefill 97% dominant)
2. `d4c3fc3` P1 axis re-rank(KV W4A8/Medusa demoted)
3. `/tmp/codex-brief-p1.0-hybrid-phase2.txt` directive(my draft,batch > 1
   suggestion)
4. Codex impl + initial threshold `batch > 1`
5. **`281d9c4`(EOD+116)Claude audit-of-audit pre-bench** — verified
   anti-pattern #25 prevention "good enough" assuming `batch > 1` semantics
6. **(this refinement,EOD+119)Codex self-correction during pre-commit
   review** — tightened to `batch > 8` matching existing decode-batched
   boundary
7. Pending:codex commit + bench result
8. Post-LAND:Claude audit-of-audit + license verdict

→ **Codex's self-correction caught a residual #25 leak that Claude's
audit-of-audit at stage 5 missed**。Anti-pattern #25 prevention is
strengthened by codex's pre-commit review applying R4 #6 KILL lesson
even more aggressively than directive specified。

This is the audit cycle compounding **at finer granularity**:not just
"is dispatch correctly env-gated" but "does dispatch boundary match
the empirically-validated cost-tradeoff inversion threshold"(batch=8
per R4 #6 KILL data)。

## Skill v1.8.0 #25 evidence reinforcement

This is the THIRD empirical evidence point for anti-pattern #25 in
the same session:
- `fe9ea8a` preliminary KILL bench(R4 #6 batch>1 leak)
- `3b9cc06` refined KILL bench(batch ∈ 2..=8 gate,confirmed Marlin wins)
- **(this refinement)** P1.0 self-correction proactively tightens to
  `batch > 8` to PREVENT same leak class from recurring

Pattern:**anti-pattern #25 is empirically a "boundary-condition cliff"
not a "yes/no syntactic check"**。The cost-tradeoff inversion happens
at a SPECIFIC batch threshold(=8 in this case),so "context match"
must align with that empirical threshold,not just intended hypothesis
target description。

→ Skill v1.8.0 #25 codification could be enhanced post-this-session
with empirical-boundary-aware sub-rule:after R4 #6 establishes
empirical inversion threshold,future axes touching same kernel
choice(Marlin vs W4A16BatchGemv vs hybrid)should respect that
threshold by default in dispatch design。

## Status

P1.0 dispatch threshold **tightened pre-commit**。Test refined to
verify env-on seq_len=2 still routes W4A16(decode-batched protection)
+ seq_len=16 triggers MarlinW4Hybrid(real prefill)。

Codex Working 37m+ on test re-run + final review。LAND imminent post
clean review pass。

Bidirectional cycle compounded into FINER-GRAIN anti-pattern #25
prevention than directive specified。Codex's pre-commit self-correction
caught residual leak class in batch 2-8 range that my EOD+116 audit
considered "acceptable per directive"。

§0 first principle:**empirical evidence**(R4 #6 KILL at batch=4)
**defines real boundaries**;hypothesis-grounded boundaries(directive's
batch > 1)may underspecify。Codex's empirical alignment is the §0
discipline applied at substrate-design layer。

## Cross-references

- `/tmp/codex-brief-p1.0-hybrid-phase2.txt` original directive
  (batch > 1 boundary)
- `281d9c4` EOD+116 audit-of-audit(batch > 1 verified "directive-faithful")
- `fe9ea8a` + `3b9cc06` R4 #6 KILL evidence(batch ∈ 2..=8 leak class)
- `aaf0b55` nsys evidence(prefill 97% dominant,P1.0 axis right)
- `6ab2293` skill v1.8.0(anti-pattern #25 codified with 2 evidence points)
